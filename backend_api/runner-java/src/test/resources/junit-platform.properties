# JUnit Platform configuration for Cucumber test discovery
# This file is automatically picked up by the JUnit Platform

# Enable Cucumber engine
cucumber.execution.dry-run=false

# Parallel execution (disabled by default for stability)
cucumber.execution.parallel.enabled=false

# Naming strategy for scenarios in reports
cucumber.plugin=pretty, summary
